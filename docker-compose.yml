version: '3.8'

services:
  web:
    build: .
    ports:
      - "5001:8000"
    volumes:
      - .:/app
      - /app/__pycache__
    environment:
      - FLASK_ENV=development
    command: gunicorn --bind 0.0.0.0:8000 --workers 1 --reload --timeout 300 app:app
