# End-to-end scenarios

All the required files are in `example_data`.

# 1. A representative is suggested after the PDB is specified

1. Upload `h-linsi.fa`
2. Fetch PDB with ID `IWX2`

**Expected outcome**: the tool selects the representative ScTyr from chain A

# 2. All functionalities

1. Upload `ppo_example.zip`
2. For a-linsi select `O76708`
3. For b-linsi select `F4PFF7`
4. Generate SVG

# 3. A fasta file is provided and then a sequence is pasted